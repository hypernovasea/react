const router = require('express').Router();
const rlController = require("../../../controllers/libr.controller");

// router.get('/all', rlController.findAllItems);
// router.get('/:id', rlController.findItem);
// router.post('/', rlController.createItem);
// router.put('/:id', rlController.updateItem); 
// router.delete('/:id', rlController.deleteItem); 

module.exports = router;